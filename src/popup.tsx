import "~style.css"

import React from "react"

import App from "./App"

function IndexPopup() {
  return (
    <div
      className={`flex items-center justify-center prose prose-indigo w-[800px]`}>
      <App />
    </div>
  )
}

export default IndexPopup
